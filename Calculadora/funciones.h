#include <stdlib.h>
#include <stdio.h>
float Ingreso(char *mensaje,float numero);
/**\brief ingreso de valor
 * \param valor de tipo cadena/char y float
 * \return valor de tipo float
 */

float Suma(float num1,float num2);
/**\brief Sumar
 * \param dos valores de tipo float
 * \return resultado de tipo float
 */

float Resta(float num1, float num2);
/**\brief Resta
 * \param dos valores de tipo float
 * \return resultado de tipo float
 */

float Division(float num1,float num2);
/**\brief Division
 * \param dos valores de tipo float
 * \return resultado de tipo float
 */

float Multiplicacion(float num1, float num2);
/**\brief multiplicar
 * \param dos valores de tipo float
 * \return resultado de tipo float
 */

float ValidarCero(float num2);
/**\brief validacion
 * \param un valor de tipo float, permite el reingreso en caso de no cumplir la condicion
 * \return numoer de tipo float
 */

float Factorial(float num1);
/**\brief Calculo y verificacion de factorial
 * \param un valore de tipo float
 * \return resultado de tipo float
 */





